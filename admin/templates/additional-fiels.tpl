<h3>Is Static</h3>

<p>Post does/does not render dynamic content.</p>

<label class="toggle_switch">
    <input type="checkbox" name="post_is_static" value="{{checked}}" {{checked === "checked" ? "checked=\"checked\""}}>
    <span class="slider"></span>
</label>